name: Build Android APK
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download UserLAndLibrary (Flattened ZIP Backup)
        run: |
          # 1. Delete any broken submodule traces
          rm -rf UserLAndLibrary
          git config --global --remove-section submodule.UserLAndLibrary || true
          rm -rf .git/modules/UserLAndLibrary || true
          
          # 2. Download the complete, flattened ZIP backup that already contains all nested code
          wget -qO UserLAndLibrary.zip https://github.com/RakhithJK/UserLAndLibrary/archive/refs/heads/master.zip
          unzip -q UserLAndLibrary.zip
          mv UserLAndLibrary-master UserLAndLibrary
          rm UserLAndLibrary.zip

      - name: The Ultimate Modern Gradle Patch
        run: |
          # 1. Strip out dead kotlin-android-extensions from EVERY build.gradle file
          find . -name "build.gradle" -exec sed -i "s/apply plugin: 'kotlin-android-extensions'//g" {} +
          find . -name "build.gradle" -exec sed -i 's/id "kotlin-android-extensions"//g' {} +
          find . -name "build.gradle" -exec sed -i "s/id 'kotlin-android-extensions'//g" {} +
          find . -name "build.gradle" -exec sed -i '/androidExtensions {/,/}/d' {} +
          
          # 2. Fix old Gradle reporting syntax globally
          find . -name "build.gradle" -exec sed -i 's/xml.enabled.*/xml.required.set(true)/g' {} +
          find . -name "build.gradle" -exec sed -i 's/html.enabled.*/html.required.set(true)/g' {} +
          
          # 3. Dynamically inject Namespace and BuildConfig into EVERY module automatically
          for manifest in $(find . -path "*/src/main/AndroidManifest.xml"); do
            module_dir=$(echo "$manifest" | sed 's|/src/main/AndroidManifest.xml||')
            build_gradle="$module_dir/build.gradle"
            if [ -f "$build_gradle" ]; then
              pkg=$(grep -oP 'package="\K[^"]+' "$manifest" | head -n 1)
              if [ ! -z "$pkg" ]; then
                sed -i "s/android {/android {\n    namespace \"$pkg\"\n    buildFeatures.buildConfig = true/g" "$build_gradle"
              fi
            fi
          done

      - name: Set up Java 
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permissions for Gradle
        run: chmod +x ./gradlew

      - name: Compile the App
        run: ./gradlew assembleDebug

      - name: Upload the compiled APK
        uses: actions/upload-artifact@v4
        with:
          name: FoxBox-APK
          path: app/build/outputs/apk/**/*.apk
